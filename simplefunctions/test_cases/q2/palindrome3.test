class: "EvalTest"
success: "isPalindrome(word) returns True if word is a palindrome, False otherwise"
failure: "isPalindrome(word) must return True if word is a palindrome, False otherwise"

# A python expression to be evaluated.  This expression must return the
# same result for the student and instructor's code.
test: "wordprops.isPalindrome('homestuck')"
