class: "EvalTest"
success: "isTopRow(word) returns True if word uses only letters on the first row of the keyboard, False otherwise"
failure: "isTopRow(word) must return True if word uses only letters on the first row of the keyboard, False otherwise"

# A python expression to be evaluated.  This expression must return the
# same result for the student and instructor's code.
test: "wordprops.isTopRow('jessica jones')"
