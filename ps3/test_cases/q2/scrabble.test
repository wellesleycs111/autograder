class: "EvalTest"
success: "scrabbleScore(word) returns the scrabble value (int) of the score"
failure: "scrabbleScore(word) must return the scrabble value (int) of the score"

# A python expression to be evaluated.  This expression must return the
# same result for the student and instructor's code.
test: "wordprops.scrabbleScore('juxtapose')"
