class: "EvalTest"
call: "unjumbleKey('argle')"
success: "returns a hash key (string with the letters of the word in sorted order) for the given word"
failure: "must return a hash key (string with the letters of the word in sorted order) for the given word"

# A python expression to be evaluated.  This expression must return the
# same result for the student and instructor's code.
test: "unjumble.unjumbleKey('argle')"
weight: "1"