class: "GradedImageTest"
call: "blueblock()"
success: "returns the blue quilt"
failure: "must return the blue quilt"

# A python expression to be evaluated.  This expression must return the
# same result for the student and instructor's code.
test: "quilt.blueblock()"
weight: "0"