class: "ImageTest"
success: "redblock() returns the red quilt"
failure: "redblock() must return the red quilt"

# A python expression to be evaluated.  This expression must return the
# same result for the student and instructor's code.
test: "quilt.redblock()"
weight: "0"
