class: "EvalTest"
success: "isTopRow('pretty') returns True"
failure: "isTopRow('pretty') must return True"

# A python expression to be evaluated.  This expression must return the
# same result for the student and instructor's code.
test: "wordprops.isTopRow('pretty')"
weight: "1"